import React from 'react';

const Calendar = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Calendar</h1>
    </div>
  );
};

export default Calendar;